# System
alias serve "python -m SimpleHTTPServer"
alias e "emacs"

## OS Spesific settings
switch (uname)
case Linux
     alias pbcopy 'xsel --clipboard --input'
end


# Git
alias ss "git status"
alias rr "git rebase"
alias gd "git diff"
alias gp "git pull"
